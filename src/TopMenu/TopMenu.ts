import { blockTopMenu } from '../variables';

const BLOCK = blockTopMenu;

import './TopMenu.styles.scss';

export function initTopMenu() {
  const selectorNavHeader = `.uc-NavHeader ${BLOCK}__adress-container .t-descr`;
  const selectorBlock = `.uc-TopMenu`;
  console.log('[TopMenu:initTopMenu]', {});
  debugger;
}
